{
  "description": "doctrine — shows each dispatched seat in the herdr sidebar while it runs. Every hook stands down immediately unless HERDR_ENV=1, so it costs a non-herdr install one process spawn per seat and nothing else.",
  "hooks": {
    "SubagentStart": [
      { "hooks": [ { "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/dctr-seat.mjs\"", "timeout": 10 } ] }
    ],
    "SubagentStop": [
      { "hooks": [ { "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/dctr-seat.mjs\"", "timeout": 10 } ] }
    ],
    "SessionEnd": [
      { "hooks": [ { "type": "command", "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/dctr-seat.mjs\"", "timeout": 10 } ] }
    ]
  }
}
